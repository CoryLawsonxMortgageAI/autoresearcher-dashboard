---
id: two-domain-evidence
when: building the evidence array
verticals: []
priority: 5
---

Every emitted finding must cite at least two URLs from two distinct
domains. Same-domain confirmation (e.g., two pages on the vendor's own
site) is not independent evidence. If you only find single-domain
sources, mark `evidenceQuality` low and prefer to skip than to emit.
